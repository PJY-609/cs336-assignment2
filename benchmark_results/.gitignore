# Superseded, incomplete latency-only run.
/h200/

# Temporary process files; completed measurements and metadata are retained.
*/jobs/
*/logs/
*/run.pid
*/run.log
**/*.tmp
# Transient process bookkeeping; manifests retain historical launch metadata.
**/.run.lock
**/launcher.pid
