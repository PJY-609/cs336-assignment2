# Compiled comparison preflight

Fourteen successful batch-size-2 validation cases across all distinct method/precision/tile regimes. Outputs and Q/K/V gradients passed; no full batch sweep was run.

Exact environment: `launch_environment.json`. Logs and job files are archived in
`../archives/compiled_comparison_partial_diagnostics.tar.gz`.
See `../h200_compile_comparison/README.md` for the stopped run and resume instructions.
